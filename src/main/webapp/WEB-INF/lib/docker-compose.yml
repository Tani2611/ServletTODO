version: '3'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: mizutani
      POSTGRES_PASSWORD: mizutani
      POSTGRES_DB: docker_postgresql
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
